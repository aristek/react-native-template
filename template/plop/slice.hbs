import { createSlice, createSelector } from "@reduxjs/toolkit";

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface {{pascalCase name}}State {}

const initialState: {{pascalCase name}}State = {};

const sliceName = "{{camelCase name}}";

const {{camelCase name}}Slice = createSlice({
  name: sliceName,
  initialState,
  reducers: {},
});

export default {{camelCase name}}Slice.reducer;

export const {{camelCase name}}Actions = {{camelCase name}}Slice.actions;

const selectState = (
  state: unknown & {
    [sliceName]: {{pascalCase name}}State;
  }
) => state;

export const {{camelCase name}}Selectors = {
  {{camelCase name}}: createSelector(selectState, (state) => state.{{camelCase name}}),
};
